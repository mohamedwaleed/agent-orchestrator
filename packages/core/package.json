{
  "name": "@orchestrator/core",
  "version": "0.1.0",
  "description": "Core orchestration engine — ticket intake, planning, wave execution, commit + PR creation.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "orchestrator": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@orchestrator/adapter-codex": "workspace:*",
    "@orchestrator/types": "workspace:*",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
